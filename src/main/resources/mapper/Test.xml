<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsgggg123.blank.dao.TestDao">
    <select id="queryById" parameterType="java.lang.Long" resultType="Test">
        select * from test
        where id = #{id}
    </select>

    <select id="queryAll" resultType="Test">
        select * from test
    </select>
</mapper>